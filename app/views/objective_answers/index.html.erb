<p style="color: green"><%= notice %></p>

<h1>Objective answers</h1>

<div id="objective_answers">
  <% @objective_answers.each do |objective_answer| %>
    <%= render objective_answer %>
    <p>
      <%= link_to "Show this objective answer", objective_answer %>
    </p>
  <% end %>
</div>

<%= link_to "New objective answer", new_objective_answer_path %>
