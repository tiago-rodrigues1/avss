<%= form_with(model: objective_answer) do |form| %>
  <% if objective_answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(objective_answer.errors.count, "error") %> prohibited this objective_answer from being saved:</h2>

      <ul>
        <% objective_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :question_alternative_id, style: "display: block" %>
    <%= form.text_field :question_alternative_id %>
  </div>

  <div>
    <%= form.label :question_id, style: "display: block" %>
    <%= form.text_field :question_id %>
  </div>

  <div>
    <%= form.label :station_submission_id, style: "display: block" %>
    <%= form.text_field :station_submission_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
