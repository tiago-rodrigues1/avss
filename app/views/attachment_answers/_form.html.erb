<%= form_with(model: attachment_answer) do |form| %>
  <% if attachment_answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(attachment_answer.errors.count, "error") %> prohibited this attachment_answer from being saved:</h2>

      <ul>
        <% attachment_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :question_id, style: "display: block" %>
    <%= form.text_field :question_id %>
  </div>

  <div>
    <%= form.label :station_submission_id, style: "display: block" %>
    <%= form.text_field :station_submission_id %>
  </div>

  <div>
    <%= form.label :payload, style: "display: block" %>
    <%= form.file_field :payload %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
