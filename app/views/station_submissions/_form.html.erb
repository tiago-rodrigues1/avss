<%= form_with(model: station_submission) do |form| %>
  <% if station_submission.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(station_submission.errors.count, "error") %> prohibited this station_submission from being saved:</h2>

      <ul>
        <% station_submission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :circuit_submission_id, style: "display: block" %>
    <%= form.text_field :circuit_submission_id %>
  </div>

  <div>
    <%= form.label :station_id, style: "display: block" %>
    <%= form.text_field :station_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
